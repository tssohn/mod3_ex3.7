
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tssohn-nginx-deployment
  namespace: tssohn-eks-activity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
        name: uploadsvc
    spec:
      serviceAccountName: tssohn-nginx-service-account
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
